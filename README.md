Mksh udh pake SC ku ya
Jangan lupa follow github ku
```bash
$ pkg update && pkg upgrade

$ pkg install git

$ pkg install python

$ pip install requests

$ pip install bs4

$ pip install futures

$ git clone https://github.com/Fanoragans/yrenc

$ ls

$ cd yrenc

$ python yrenc.py
```
